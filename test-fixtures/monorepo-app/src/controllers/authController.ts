import { Request, Response } from "express";
export async function login(req: Request, res: Response) { return res.json({}); }
export async function register(req: Request, res: Response) { return res.json({}); }
export async function refreshToken(req: Request, res: Response) { return res.json({}); }
