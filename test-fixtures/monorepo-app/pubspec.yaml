name: monorepo_mobile
description: Mobile client in a Flutter+Express monorepo

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
